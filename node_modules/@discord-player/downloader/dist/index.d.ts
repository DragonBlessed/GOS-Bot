import * as stream from 'stream';
import * as youtubeDlExec from 'youtube-dl-exec';
export { youtubeDlExec as ytdl };

interface Info {
    title: string;
    duration: number;
    thumbnail: string;
    views: number;
    author: string;
    description: string;
    url: string;
    source: string;
    engine: stream.Readable;
}
declare class Downloader {
    constructor();
    /**
     * Downloads stream through youtube-dl
     * @param {string} url URL to download stream from
     */
    static download(url: string): stream.Readable;
    /**
     * Returns stream info
     * @param {string} url stream url
     */
    static getInfo(url: string): Promise<{
        playlist: any;
        info: Info[];
    }>;
    static validate(url: string): boolean;
    static get important(): boolean;
}

declare const version: string;

export { Downloader, Info, version };
